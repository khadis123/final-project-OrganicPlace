import { useEffect, useState } from "react";
import styled from "styled-components";
import { useNavigate } from "react-router-dom";
import { Link } from "react-router-dom";

const Checkout = () => {
//what fetches I need for checkout page? As this is a signed in user,
// I cannot send the items data via formData as in previous excercises. 
// Then what's the best approach in my case?

    return (
        <>
        
        </>
    )
}

export default Checkout;

